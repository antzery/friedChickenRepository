using UnityEngine;

public class bagongScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("The world is being hello-ed");
    }

    // Update is called once per frame
    void Update()
    {
        print("H ell o W orld !");
    }
}
